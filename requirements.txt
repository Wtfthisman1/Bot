# Зависимости образа Docker. Дома окружение другое — /home/dmitry/whisper-env
# с torch под CUDA и pyannote для диаризации; здесь всё под CPU.
#
# Версии закреплены до точки. Без этого каждая пересборка образа тянула свежее
# с PyPI: содержимое зависело от дня сборки, воспроизвести старый образ было
# нечем, а подмена пакета в реестре приезжала бы молча — и yt-dlp с ffmpeg
# работают ровно на тех данных, которые прислал посторонний.
#
# Как поднимать версию: поменять число здесь, пересобрать образ, прогнать одну
# расшифровку и одно скачивание. Не «pip install -U» на живой машине.

# Core ML dependencies
#
# numpy остаётся первой ветки: torch 2.2.1 собран под неё, со второй он падает
# на импорте. «<2» здесь было тем же незакреплённым диапазоном
numpy==1.26.4
torch==2.2.1+cpu
-f https://download.pytorch.org/whl/torch_stable.html

# Whisper and audio processing
faster-whisper==1.0.1
# faster-whisper 1.0.1 требует huggingface-hub>=0.13 — верхней границы у него
# нет, а мажорная версия хаба меняет API загрузки моделей
huggingface-hub==0.34.4

# Video downloading
#
# Единственная зависимость, которую придётся двигать часто: yt-dlp живёт
# правками под изменения площадок, и версия полугодовой давности перестаёт
# качать. Это не повод не закреплять её — это повод обновлять осознанно,
# проверяя скачивание, а не узнавать о смене кода в момент пересборки.
yt-dlp==2026.8.19

# Utilities
tqdm==4.67.3
