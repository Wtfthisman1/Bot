-- Куда прислать готовую обработку, если её заказали не со страницы.

-- Кнопка «Выжимка» появилась под расшифровкой в чате, а модель считает
-- минутами: к моменту готовности человек уже занят другим, и ответ должен сам
-- прийти туда, где его попросили.
--
-- Колонка, а не вывод по владельцу задачи: у аккаунта на сайте может быть
-- привязан чат, и тогда выжимка, заказанная в браузере, приходила бы ещё и
-- в Telegram. Пустое значение здесь означает «показать на странице» — ровно
-- то, что было до этой колонки.
alter table transcript_insights
    add column notify_chat_id bigint;
