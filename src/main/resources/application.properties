spring.application.name=Bot

# ========================================
# ПРОФИЛИ: ГДЕ ЧТО ПОДНЯТО
# ========================================
# home — очередь, база, файлы, yt-dlp и Whisper; bot — приём запросов
# (long polling Telegram, дальше сайт). По умолчанию оба: это привычный запуск
# на одной машине, и обновление не должно ломать существующие установки.
# На VPS остаётся SPRING_PROFILES_ACTIVE=bot, дома — home.
spring.profiles.default=bot,home

# Базы в этом процессе нет — и это состояние по умолчанию. Автоконфигурация
# иначе ищет DataSource и роняет контекст на старте, а боту на VPS база не
# нужна: задачи он ставит через HTTP, у дома. Профиль home возвращает всё
# обратно — см. application-home.properties.
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,\
  org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration,\
  org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration

# Адрес и общий ключ домашней машины. Нужны только там, где профиля home нет:
# бот на VPS ходит по ним через WireGuard. Дом тем же ключом проверяет входящие.
home.api.base-url=${HOME_API_URL:}
home.api.key=${HOME_API_KEY:}

# ========================================
# СПУЛ: ЗАДАЧИ, ПРИНЯТЫЕ ПРИ СПЯЩЕМ ДОМЕ
# ========================================
# Работает только там, где нет профиля home. Задача ложится файлом на диск и
# уходит домой при первом же успешном обращении.
spool.enabled=true
spool.dir=${SPOOL_DIR:${user.dir}/spool}
# Как часто пробовать отдать накопленное. Минута — компромисс: дом просыпается
# не по расписанию, а чаще дёргать выключенную машину бессмысленно.
spool.flush-interval-ms=60000
# Предел на случай, если дом не включался неделями: диск VPS — 10 ГБ, и
# бесконечная очередь всё равно означает, что что-то не так
spool.max-tasks=500
# Задачу трёхдневной давности повторять поздно: человек про неё забыл
spool.max-age-days=3

# ========================================
# СТАТИЧЕСКИЕ РЕСУРСЫ
# ========================================
# Форму upload.html отдаёт UploadController по одноразовому токену, поэтому
# отдельный WebConfig с маппингом /** больше не нужен — здесь только запрет кэша,
# чтобы правки формы были видны без пересборки образа.
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false

# ========================================
# НАСТРОЙКИ БОТА (значения — через переменные окружения)
# ========================================
bot.name=${BOT_NAME:FindInform_rail_bot}
# Токен обязателен: без него запуск не имеет смысла — пусть падает на старте
bot.key=${BOT_TOKEN}
# Пустой дефолт: если ADMIN_CHAT_ID не задан, отправка ошибок в Telegram просто отключится
# (BotInitializer обрабатывает пустое/некорректное значение, не роняя контекст)
admin.chat.id=${ADMIN_CHAT_ID:}

spring.devtools.restart.enabled=false

# ========================================
# ССЫЛКИ (upload/download)
# ========================================
# ВАЖНО: в проде задайте публичный адрес через UPLOAD_BASE_URL / DOWNLOAD_BASE_URL —
# по этим ссылкам пользователь открывает форму и качает файлы из Telegram.
# localhost-дефолты годятся только для локальной разработки.
upload.base-url=${UPLOAD_BASE_URL:http://localhost:8080}
download.base-url=${DOWNLOAD_BASE_URL:http://localhost:8080}

# ========================================
# КВОТА НА РАСШИФРОВКИ
# ========================================
# Считается по аккаунту: сайт и привязанная переписка тратят один лимит.
# Скачивание файлов в него не входит — видеокарта на нём не работает.
quota.enabled=${QUOTA_ENABLED:true}
quota.free-transcriptions-per-month=${QUOTA_FREE_PER_MONTH:3}
# По этому поясу месяц считается начавшимся: по UTC лимит обновлялся бы
# первого числа не с утра, а посреди ночи предыдущего дня
quota.zone=${QUOTA_ZONE:Europe/Moscow}

# ========================================
# ВХОД ЧЕРЕЗ GOOGLE
# ========================================
# Пусто — кнопки «Войти через Google» просто нет (см. GoogleClientConfig).
# Адрес возврата для консоли Google Cloud:
#   https://<домен>/login/oauth2/code/google
google.client-id=${GOOGLE_CLIENT_ID:}
google.client-secret=${GOOGLE_CLIENT_SECRET:}

# ========================================
# БЕЗОПАСНОСТЬ ТОКЕНОВ
# ========================================
# Одноразовые ссылки на форму загрузки
upload.token.length=8
upload.token.chars=ABCDEFGHIJKLMNOPQRSTUVWXYZ0REDACTED_CHAT_ID
upload.token.ttl.hours=1
# Срок жизни ссылки на скачивание готового файла (DownloadTokenRegistry)
download.token.ttl-hours=24
# Максимальный размер файла, отправляемого прямо в чат (лимит Bot API — 50 МБ)
download.telegram-max-bytes=52428800

# ========================================
# ХРАНИЛИЩЕ ФАЙЛОВ
# ========================================
app.storage.base=${user.dir}/upload/videos

spring.servlet.multipart.max-file-size=2500MB
spring.servlet.multipart.max-request-size=2500MB
spring.servlet.multipart.file-size-threshold=2KB
spring.servlet.multipart.location=${java.io.tmpdir}

# ========================================
# ОЧЕРЕДЬ И ОБРАБОТКА ЗАДАЧ
# ========================================
# Количество воркеров, разбирающих очередь (JobWorker).
# Транскрипция CPU-bound (Whisper уже многопоточный) — крупные значения вызовут
# конкуренцию за CPU. Поднимать осторожно и под конкретное железо.
worker.pool-size=2
# Таймаут одной транскрипции (минуты). Длинное видео на CPU может идти часами.
whisper.timeout-minutes=300

# ========================================
# ПЛАНИРОВЩИК И ПУЛЫ SPRING
# ========================================
spring.task.scheduling.pool.size=4
spring.task.execution.pool.core-size=4
spring.task.execution.pool.max-size=8
spring.task.execution.pool.queue-capacity=100

# ========================================
# ВЕБ-СЕРВЕР (Tomcat)
# ========================================
# За обратным прокси Tomcat должен доверять X-Forwarded-*, иначе в логах
# вместо адреса пользователя будет адрес прокси
server.forward-headers-strategy=FRAMEWORK

server.tomcat.threads.max=200
server.tomcat.threads.min-spare=10
server.tomcat.max-connections=8192
server.tomcat.accept-count=100

# ========================================
# ОЧИСТКА ФАЙЛОВ
# ========================================
cleanup.enabled=true
cleanup.retention-days=14
cleanup.schedule=0 0 2 * * ?

# ========================================
# MONITORING / HEALTH
# ========================================
# Actuator слушает ОТДЕЛЬНЫЙ порт и только на localhost. Так его физически
# нельзя выставить наружу, даже если ошибиться в конфиге обратного прокси:
# health/metrics/prometheus доступны с самой машины, но не из интернета.
management.server.port=${MANAGEMENT_PORT:8081}
management.server.address=127.0.0.1
management.endpoints.web.base-path=/actuator
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=when-authorized
management.endpoint.health.show-components=when-authorized
management.endpoint.shutdown.enabled=false
# Spring Boot 3.x / Micrometer: проперти переименовано
# (было management.metrics.export.prometheus.enabled)
management.prometheus.metrics.export.enabled=true

# ========================================
# ЛОГИРОВАНИЕ
# ========================================
# logging.level.Bot здесь НЕ задаётся сознательно: Spring Boot применяет его
# после загрузки logback.xml и перекрыл бы уровень логгера. Пакет Bot держится
# на DEBUG в logback.xml, потому что построчный вывод yt-dlp и Whisper должен
# попадать в лог задачи (logs/jobs/job-<id>.log). Видимостью в консоли и
# logs/app.log управляет переменная LOG_LEVEL через ThresholdFilter.
logging.level.org.springframework.web=WARN
logging.level.org.telegram=WARN
# Паттерн консоли живёт в logback.xml (там же MDC chatId/jobId) — здесь не дублируем

# ========================================
# НАСТРОЙКИ PYTHON (Виртуальное окружение)
# ========================================
# Путь к Python внутри виртуального окружения
python.executable.path=/home/dmitry/whisper-env/bin/python3

whisper.model=large-v3
whisper.threads=6
# auto — whisper определяет язык сам. Раньше здесь всегда стоял русский, из-за
# чего англоязычная запись расшифровывалась как русская. Определённый язык
# нужен ещё и для выбора языковой модели при обработке текста.
whisper.language=${WHISPER_LANGUAGE:auto}
whisper.clean-audio=false
whisper.initial-prompt=
whisper.ct-binary=/home/dmitry/whisper-env/bin/whisper-ctranslate2
whisper.script=classpath:pythonScript/Transcribe.py
downloader.script=classpath:pythonScript/Downloader.py
# Каталог с deno: без JS-рантайма свежий yt-dlp не тянет YouTube (403).
# В Docker/systemd задайте через DOWNLOADER_JS_RUNTIME_DIR, если deno лежит не здесь.
downloader.js-runtime-dir=${DOWNLOADER_JS_RUNTIME_DIR:${user.home}/.deno/bin}
# Указываем использование видеокарты (Compute Unified Device Architecture от NVIDIA)
whisper.device=cuda

# Для видеокарт рекомендуется использовать float16 (работает быстрее и точнее на GPU)
whisper.compute-type=float16

# ========================================
# ДИАРИЗАЦИЯ (кто говорит)
# ========================================
# Второй проход по тому же звуку уже после Whisper — тот освободил видеокарту,
# и 8 ГБ не приходится делить. Выключена по умолчанию: без токена Hugging Face
# модель pyannote просто не скачается, а расшифровки должны идти и без неё.
diarize.enabled=${DIARIZE_ENABLED:false}
diarize.hf-token=${HF_TOKEN:}
diarize.model=${DIARIZE_MODEL:pyannote/speaker-diarization-3.1}
# Границы числа голосов; пусто — pyannote решает сам. Задавать имеет смысл
# только там, где число собеседников известно заранее (интервью, подкаст)
diarize.min-speakers=${DIARIZE_MIN_SPEAKERS:}
diarize.max-speakers=${DIARIZE_MAX_SPEAKERS:}

# ========================================
# ОБРАБОТКА ТЕКСТА МОДЕЛЬЮ (выжимка, разбор по теме)
# ========================================
# Считает Ollama на этой же машине. Выключено по умолчанию: без установленной
# службы и скачанной модели кнопки на странице расшифровки просто не появятся,
# а всё остальное работает как прежде.
insight.enabled=${INSIGHT_ENABLED:false}
# Только localhost: открытая в сеть модель — это чужие запросы на моей видеокарте
insight.url=${OLLAMA_URL:http://127.0.0.1:11434}
insight.model=${INSIGHT_MODEL:qwen2.5:7b-instruct}
# Окно модели в токенах. Больше — дороже по видеопамяти, которую делим с Whisper
insight.num-ctx=${INSIGHT_NUM_CTX:8192}
# Сколько символов расшифровки уходит за один заход: в окно кроме куска должны
# поместиться правила, вопрос и ответ. 6000 — это примерно 15 минут разговора
insight.chunk-chars=${INSIGHT_CHUNK_CHARS:6000}
# Предел на один вопрос к модели. Кусок длинной записи на 8 ГБ идёт минутами
insight.timeout-minutes=${INSIGHT_TIMEOUT_MINUTES:20}
# Сколько держать веса в видеопамяти между вопросами одной обработки. После неё
# воркер выгружает их сразу — иначе следующая расшифровка ждала бы впустую
insight.keep-alive-minutes=${INSIGHT_KEEP_ALIVE_MINUTES:5}
